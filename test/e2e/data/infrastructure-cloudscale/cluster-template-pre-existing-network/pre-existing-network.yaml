---
apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
kind: CloudscaleCluster
metadata:
  name: "${CLUSTER_NAME}"
  namespace: "${NAMESPACE}"
spec:
  networks:
  - name: "${CLUSTER_NAME}"
    uuid: "${CLOUDSCALE_NETWORK_UUID}"
